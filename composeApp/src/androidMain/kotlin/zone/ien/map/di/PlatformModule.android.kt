package zone.ien.map.di

import org.koin.dsl.module

actual val platformModule = module {
//    single<NetworkHelper> { AndroidNetworkHelper(androidContext()) }
//    single<AppTerminator> { AndroidAppTerminator() }
}